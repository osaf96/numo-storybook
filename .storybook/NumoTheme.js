import { create } from '@storybook/theming/create';

export default create({
    base: 'light',
    brandTitle: 'Numo Storybook',
    brandUrl: '/',
    brandImage: 'numo.svg',
    brandTarget: '_self',
});